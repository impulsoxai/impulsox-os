# Skill `/slides` — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir a skill `/slides` — gera um deck de apresentação premium (HTML navegável em tela cheia, marca cravada) pra rodar no PC durante a gravação de vídeo, com produto real em mockup, slides-ponte pra demo ao vivo no Claude Code e notas do apresentador.

**Architecture:** A skill é declarativa: um `SKILL.md` com o fluxo guiado (2 gates) + arquivos de referência que são o MOTOR do deck (um `engine.html` auto-contido com navegação/presenter-view, e um `blocos.md` com os 5 tipos de slide). Em runtime, dentro do clone, a skill lê o núcleo + `marca/tokens.css`, escreve o conteúdo, injeta os tokens da marca no engine e cospe `producao/slides/<tema>/deck.html`. Não há código de aplicação rodando em CI — o "teste" de cada artefato é renderizá-lo num navegador headless (Playwright, já usado no projeto) e auditar o resultado visualmente, exatamente como o `/pagina` e o `/reel-marca` verificam.

**Tech Stack:** HTML5 + CSS moderno (custom properties, grid, clamp) + JavaScript vanilla (engine de navegação ~150 linhas). Playwright pra verificação visual (já no projeto). Sem framework de slides (CLAUDE.md proíbe estética default).

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `.claude/skills/slides/SKILL.md` | Fluxo guiado da skill: lê núcleo, detecta entrada, 2 gates, constrói, abre. |
| `.claude/skills/slides/references/engine.html` | Shell auto-contido do deck: CSS de marca (via custom properties), JS de navegação (setas/F/S/B/Home/End), presenter view, slot pros slides. É o molde que a skill copia e preenche. |
| `.claude/skills/slides/references/blocos.md` | Os 5 tipos de slide (capa, conteúdo, produto-real, ponte-demo, assinatura) como snippets HTML prontos pra colar no engine. |
| `.claude/skills/slides/references/verificar.mjs` | Script Playwright que abre o `deck.html`, navega slide a slide, tira screenshot de cada um + do presenter view. Usado na verificação e disponível pra skill em runtime. |
| `docs/mapa-de-skills.md` | Registrar `/slides` na esteira vídeo + na tabela de fluxo guiado. |
| `CLAUDE.md` | Bump de versão (v0.2.6 → v0.2.7). |

Saída em runtime (NÃO criada por este plano — é trabalho de marketing no clone): `producao/slides/<tema>/deck.html` + assets + `notas.md`.

**Diretório de trabalho de verificação:** use o scratchpad `C:\Users\ACER\AppData\Local\Temp\claude\c--Users-ACER-Desktop-ImpulsoX-OS\b81284f1-ff65-4614-a191-a6afd5e82c6c\scratchpad` pra qualquer deck de teste. Nunca commitar deck de teste.

---

## Task 1: Engine do deck (navegação + presenter view)

O coração técnico. Um HTML auto-contido com tokens de marca por custom property, navegação por teclado, presenter view e blackout. É o molde que a skill preenche depois.

**Files:**
- Create: `.claude/skills/slides/references/engine.html`
- Verify (scratchpad): `…/scratchpad/deck-teste.html`

- [ ] **Step 1: Escrever o `engine.html`**

Conteúdo completo do arquivo (é um molde: a skill troca `/* TOKENS_DA_MARCA */` pelos valores reais do `marca/tokens.css` do clone, e `<!-- SLIDES -->` pelos slides montados a partir do `blocos.md`):

```html
<!doctype html>
<html lang="pt-BR">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Deck — ImpulsoX</title>
<style>
  :root{
    /* TOKENS_DA_MARCA — substituídos pela skill a partir de marca/tokens.css.
       Defaults premium escuros enquanto não há marca (Escada de Contexto). */
    --bg:#080510; --fg:#f5f3ff; --muted:#a99fc4;
    --accent:#a974ff; --accent-2:#e9cd6a;
    --font-display:'Georgia',serif; --font-body:system-ui,sans-serif;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  html,body{height:100%}
  body{
    background:var(--bg);color:var(--fg);font-family:var(--font-body);
    overflow:hidden;
  }
  /* glow linear discreto do topo — regra premium do reel (nunca orb radial) */
  body::before{
    content:"";position:fixed;inset:0;pointer-events:none;z-index:0;
    background:linear-gradient(180deg,transparent 0%,color-mix(in srgb,var(--accent) 14%,transparent) 20%,transparent 52%);
  }
  .deck{position:relative;z-index:1;height:100vh;width:100vw}
  .slide{
    position:absolute;inset:0;display:none;
    padding:clamp(3rem,8vw,7rem);
    flex-direction:column;justify-content:center;gap:1.5rem;
  }
  .slide.is-active{display:flex;animation:fade .5s ease both}
  @keyframes fade{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion:reduce){.slide.is-active{animation:none}}
  h1,h2{font-family:var(--font-display);line-height:1.05;letter-spacing:-.02em}
  h1{font-size:clamp(2.5rem,7vw,5.5rem)}
  h2{font-size:clamp(2rem,5vw,4rem)}
  p,li{font-size:clamp(1.1rem,2.2vw,1.6rem);color:var(--muted);max-width:60ch}
  .accent{color:var(--accent)}
  .kicker{text-transform:uppercase;letter-spacing:.18em;font-size:.85rem;color:var(--accent-2)}
  /* progresso */
  .progress{position:fixed;left:0;bottom:0;height:4px;background:var(--accent);z-index:5;transition:width .3s}
  /* presenter view */
  .presenter{
    position:fixed;inset:0;z-index:10;background:#000;color:#fff;display:none;
    grid-template-columns:1fr 1fr;gap:2rem;padding:2.5rem;font-family:var(--font-body);
  }
  .presenter.is-on{display:grid}
  .presenter h3{font-size:1rem;color:var(--accent-2);letter-spacing:.12em;text-transform:uppercase;margin-bottom:1rem}
  .presenter .notes{font-size:1.4rem;line-height:1.5;white-space:pre-wrap}
  .presenter .next{opacity:.6;font-size:1.1rem}
  .blackout{position:fixed;inset:0;background:#000;z-index:20;display:none}
  .blackout.is-on{display:block}
  .hint{position:fixed;right:1rem;top:1rem;z-index:6;font-size:.75rem;color:var(--muted);opacity:.5}
</style>
</head>
<body>
  <div class="deck" id="deck">
    <!-- SLIDES — substituído pela skill. Cada slide:
         <section class="slide" data-notes="nota do apresentador">…conteúdo…</section> -->
    <section class="slide"><h1>Deck vazio</h1><p>Substitua pelos slides.</p></section>
  </div>
  <div class="progress" id="progress"></div>
  <div class="hint">→ ← passar · F tela cheia · S notas · B preto</div>
  <div class="presenter" id="presenter">
    <div><h3>Notas do apresentador</h3><div class="notes" id="pNotes"></div></div>
    <div><h3>Próximo slide</h3><div class="next" id="pNext"></div></div>
  </div>
  <div class="blackout" id="blackout"></div>
<script>
  const slides=[...document.querySelectorAll('.slide')];
  let i=0;
  const progress=document.getElementById('progress');
  const presenter=document.getElementById('presenter');
  const pNotes=document.getElementById('pNotes');
  const pNext=document.getElementById('pNext');
  const blackout=document.getElementById('blackout');
  function show(n){
    i=Math.max(0,Math.min(slides.length-1,n));
    slides.forEach((s,k)=>s.classList.toggle('is-active',k===i));
    progress.style.width=((i+1)/slides.length*100)+'%';
    pNotes.textContent=slides[i].dataset.notes||'(sem nota)';
    pNext.textContent=slides[i+1]?(slides[i+1].textContent.trim().slice(0,120)):'— fim —';
  }
  document.addEventListener('keydown',e=>{
    if(e.key==='ArrowRight'||e.key===' '){show(i+1);e.preventDefault();}
    else if(e.key==='ArrowLeft'){show(i-1);}
    else if(e.key==='Home'){show(0);}
    else if(e.key==='End'){show(slides.length-1);}
    else if(e.key==='f'||e.key==='F'){document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen();}
    else if(e.key==='s'||e.key==='S'){presenter.classList.toggle('is-on');}
    else if(e.key==='b'||e.key==='B'){blackout.classList.toggle('is-on');}
  });
  show(0);
</script>
</body>
</html>
```

- [ ] **Step 2: Copiar pro scratchpad e abrir num navegador headless pra verificar que carrega**

Run:
```bash
cp .claude/skills/slides/references/engine.html "$SCRATCH/deck-teste.html"
node -e "const{chromium}=require('playwright');(async()=>{const b=await chromium.launch();const p=await b.newPage();await p.setViewportSize({width:1280,height:720});await p.goto('file://'+process.env.SCRATCH.replace(/\\\\/g,'/')+'/deck-teste.html');await p.screenshot({path:process.env.SCRATCH+'/s0.png'});await b.close();})()"
```
(Defina `SCRATCH` = caminho do scratchpad. `playwright` já está no projeto — se `require` falhar, rode de dentro de `ImpulsoX-AI` onde está instalado, ou `npx playwright`.)

Expected: gera `s0.png` sem erro; a imagem mostra "Deck vazio" em fundo escuro com glow no topo.

- [ ] **Step 3: Verificar navegação por teclado (próximo slide / progresso)**

Run:
```bash
node -e "const{chromium}=require('playwright');(async()=>{const b=await chromium.launch();const p=await b.newPage();await p.goto('file://'+process.env.SCRATCH.replace(/\\\\/g,'/')+'/deck-teste.html');const w0=await p.$eval('#progress',e=>e.style.width);await p.keyboard.press('ArrowRight');const w1=await p.$eval('#progress',e=>e.style.width);console.log('antes',w0,'depois',w1);await b.close();})()"
```
Expected: imprime `antes 100% depois 100%` (1 slide só → fica no fim; navegação não quebra). O teste real de navegação multi-slide vem na Task 2.

- [ ] **Step 4: Commit**

```bash
git add .claude/skills/slides/references/engine.html
git commit -m "feat(slides): engine do deck — navegação, presenter view, tokens de marca

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Blocos de slide (os 5 tipos) + verificação multi-slide

Os snippets de cada tipo de slide e um deck de teste com os 5, pra provar que navegação, mockup, ponte-demo e notas funcionam juntos.

**Files:**
- Create: `.claude/skills/slides/references/blocos.md`
- Create: `.claude/skills/slides/references/verificar.mjs`
- Verify (scratchpad): `…/scratchpad/deck-5tipos.html`

- [ ] **Step 1: Escrever `blocos.md` com os 5 tipos**

Conteúdo do arquivo:

````markdown
# Blocos de slide — `/slides`

Cada bloco é um `<section class="slide" data-notes="…">` pra colar no `<!-- SLIDES -->` do
`engine.html`. `data-notes` = nota do apresentador (escondida na projeção; aparece no presenter
view com `S` e no `notas.md`). Regra de ouro herdada do `/reel-marca`: **1 herói por slide,
muito espaço negativo, answer-first**. Sem parede de texto.

## 1. capa
```html
<section class="slide" data-notes="Abertura. Falar o gancho do vídeo em 1 frase.">
  <p class="kicker">[NOME DA MARCA]</p>
  <h1>[Título do vídeo]<br><span class="accent">[ênfase]</span></h1>
</section>
```

## 2. conteúdo (1 ideia, answer-first)
```html
<section class="slide" data-notes="[o que falar aqui]">
  <p class="kicker">[seção]</p>
  <h2>[Headline que já é a resposta]</h2>
  <p>[1 frase de apoio — no máximo. Não encher.]</p>
</section>
```

## 3. produto-real (screenshot em mockup)
Phone (carrossel) ou Browser (página). `src` aponta pro asset copiado de `producao/`.
```html
<section class="slide" data-notes="[contar o que esse produto resolve]">
  <h2>[O que isso mostra]</h2>
  <div class="mockup mockup--phone">
    <img src="assets/[arquivo].png" alt="[descrição real]">
  </div>
</section>
```
CSS dos mockups (a skill garante que está no `<style>` do deck — adicionar ao engine ao montar):
```css
.mockup--phone{width:min(34vh,300px);aspect-ratio:9/19.5;border:10px solid #1a1430;border-radius:36px;overflow:hidden;box-shadow:0 30px 80px rgba(0,0,0,.5);align-self:center}
.mockup--browser{width:min(70vw,900px);aspect-ratio:16/10;border:1px solid #2a2342;border-radius:12px;overflow:hidden;box-shadow:0 30px 80px rgba(0,0,0,.5);align-self:center;background:#0c0a16}
.mockup img{width:100%;height:100%;object-fit:cover;object-position:top}
```

## 4. ponte-demo (sinal de Alt+Tab pro Claude Code)
```html
<section class="slide" data-notes="DEMO AO VIVO: [passo a passo do que fazer no Claude Code]. Voltar pro deck quando terminar.">
  <p class="kicker">▶ demo ao vivo</p>
  <h2><span class="accent">[ação]</span> ao vivo no Claude Code</h2>
  <p>[1 linha do que o espectador vai ver]</p>
</section>
```

## 5. assinatura (fecho institucional)
```html
<section class="slide" data-notes="Fecho. Sem CTA gritado — entrega calma.">
  <h2>[Marca]</h2>
  <p class="kicker">[kicker institucional — ex: Agência de IA pra PME]</p>
  <p>[url]</p>
</section>
```
````

- [ ] **Step 2: Montar um deck de teste com os 5 tipos no scratchpad**

Crie `$SCRATCH/deck-5tipos.html` copiando o `engine.html` e substituindo o `<!-- SLIDES -->` (e o comentário do slide vazio) pelos 5 blocos preenchidos com texto de exemplo, e adicione o CSS dos mockups ao `<style>`. Use uma imagem qualquer existente como `assets/[arquivo].png` (ex: copie um `marca/logo/*.png` pra `$SCRATCH/assets/`). Conteúdo de exemplo: capa "ImpulsoX / IA que trabalha", conteúdo "Você sumiu das redes", produto-real (phone), ponte-demo "gerar landing page", assinatura.

- [ ] **Step 3: Escrever `verificar.mjs` (Playwright: screenshot de cada slide + presenter)**

Conteúdo do arquivo:

```javascript
// Uso: node verificar.mjs <caminho-do-deck.html> <dir-saida>
// Abre o deck, navega slide a slide tirando screenshot de cada um, e um do presenter view.
import { chromium } from 'playwright';
import { pathToFileURL } from 'url';
import { mkdirSync } from 'fs';
import { resolve } from 'path';

const deck = process.argv[2];
const outDir = process.argv[3] || '.';
if (!deck) { console.error('uso: node verificar.mjs <deck.html> <dir-saida>'); process.exit(1); }
mkdirSync(outDir, { recursive: true });

const browser = await chromium.launch();
const page = await browser.newPage();
await page.setViewportSize({ width: 1280, height: 720 });
await page.goto(pathToFileURL(resolve(deck)).href);

const total = await page.$$eval('.slide', s => s.length);
console.log('slides:', total);
for (let n = 0; n < total; n++) {
  await page.screenshot({ path: resolve(outDir, `slide-${String(n + 1).padStart(2, '0')}.png`) });
  await page.keyboard.press('ArrowRight');
  await page.waitForTimeout(250);
}
await page.keyboard.press('Home');
await page.keyboard.press('s'); // presenter view
await page.waitForTimeout(150);
await page.screenshot({ path: resolve(outDir, 'presenter.png') });
await browser.close();
console.log('ok →', outDir);
```

- [ ] **Step 4: Rodar a verificação e conferir os screenshots**

Run:
```bash
node .claude/skills/slides/references/verificar.mjs "$SCRATCH/deck-5tipos.html" "$SCRATCH/shots"
```
Expected: imprime `slides: 5` e `ok → …/shots`; gera `slide-01.png`..`slide-05.png` + `presenter.png`. Abrir as imagens (Read tool) e confirmar: cada tipo renderiza, o mockup mostra a imagem, a ponte-demo tem o "▶ demo ao vivo", e `presenter.png` mostra as notas. Se algum slide vier quebrado/preto, corrigir o bloco/engine antes de seguir.

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/slides/references/blocos.md .claude/skills/slides/references/verificar.mjs
git commit -m "feat(slides): 5 tipos de slide + verificação visual Playwright

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: SKILL.md — o fluxo guiado

A skill em si: como lê contexto, detecta entrada, os 2 gates, constrói o deck injetando a marca, abre no navegador.

**Files:**
- Create: `.claude/skills/slides/SKILL.md`

- [ ] **Step 1: Escrever o `SKILL.md`**

Conteúdo do arquivo:

```markdown
---
name: slides
description: >
  Use pra criar um deck de apresentação premium (slides em tela cheia, na marca do negócio) pra
  rodar no PC enquanto grava vídeo — "/slides", "faz uns slides", "apresentação pro vídeo", "deck
  pra gravação", "slides com a minha marca". Gera um HTML navegável (setas, tela cheia, presenter
  view) com produto real em mockup, slides-ponte pra demo ao vivo no Claude Code e notas do
  apresentador. Aceita tema em 1 frase, roteiro colado, ou roteiro de /roteiro-yt·/tema-yt
  (Escada de Contexto). Distinta do /reel-marca (vídeo que toca sozinho) — aqui o dono NAVEGA
  ao vivo. Tem modo institucional (pitch ImpulsoX pronto) e modo tema (deck do vídeo da vez).
---

# /slides — Deck de apresentação premium pra gravação

Gera um deck de slides em tela cheia, na identidade visual do negócio, pra rodar no navegador
durante a gravação de um vídeo. O dono passa os slides no clique e alterna pro Claude Code pra
mostrar o produto real sendo feito. Premium nível-agência: a marca é cravada, nunca tem cara de
template (CLAUDE.md proíbe estética default).

Autoria: ImpulsoX AI.

## O que NÃO é
- Não é vídeo que toca sozinho — isso é o `/reel-marca`. Aqui o dono navega ao vivo.
- Não é overlay de OBS — é deck de tela cheia que o dono passa no clique.

## Pré-requisitos (Escada de Contexto)
- **Marca** (`marca/tokens.css`, `marca/design-guide.md`) — pro deck sair na identidade do cliente.
  Sem ela: defaults premium escuros marcados "confirmar com a marca" (não trava); ofereça
  `/identidade` antes.
- **Núcleo** (`nucleo/negocio.md`, `nucleo/voz.md`, `nucleo/ofertas.md`, `nucleo/provas.md`) —
  pro conteúdo sair na voz do dono e só vender oferta ATIVA.
- **Produto real** (opcional, melhora muito): peças em `producao/` (carrosséis, páginas) pra
  mostrar em mockup. Sem nada: slides de texto.
- **Node + Playwright** pra a verificação visual (já no projeto).

## Fluxo (guiado — 2 gates de aprovação)

1. **Lê o contexto** (silêncio): núcleo + `marca/tokens.css` + `marca/design-guide.md`.

2. **Detecta a entrada** (Escada de Contexto, os 3 níveis):
   - tema em 1 frase → puxa o núcleo e escreve o roteiro dos slides na voz do dono;
   - roteiro colado → vira slides direto;
   - vindo de `/roteiro-yt`·`/tema-yt` → transforma o roteiro em slides;
   - nada informado → **modo institucional** (pitch da ImpulsoX: o que ela faz com Claude Code,
     ofertas ATIVAS, produto real). Salvo/versionado como deck-base.

3. **Escolhe o produto real:** varre `producao/` (carrosséis em `posts/*/slide-*.png`, páginas),
   casa com o tema, marca quais entram em mockup. Sem produto → slide de texto.

4. **GATE 1 — storyboard.** Mostra o esqueleto numa tabela: para cada slide, o tipo (capa/
   conteúdo/produto-real/ponte-demo/assinatura), o headline, onde entra cada ponte-demo e qual
   produto real aparece. Espera o "sim". (Regra do `/reel-marca`: alinhar a estrutura antes de produzir.)

5. **Escreve a copy** — headlines answer-first, 1 ideia por slide, na voz do dono → passa pelo
   `/escritor-br` (humaniza). **Só oferta ATIVA** (CLAUDE.md — peça pública não vende oferta
   futura/roadmap, nem "em breve"). Escreve as notas do apresentador (`data-notes`) aqui:
   nas pontes-demo, o passo a passo do que fazer no Claude Code.

6. **GATE 2 — copy completa.** Mostra TODO o texto de tela + as notas, slide a slide, numa
   tabela. Espera o "sim". **Nunca construir com texto não aprovado.** (Regra do dono no
   `/reel-marca`: "antes de fazer, me passa sempre o que vai escrever".)

7. **Constrói o deck:**
   - copia `references/engine.html` pra `producao/slides/<tema>/deck.html`;
   - lê `marca/tokens.css` e substitui o bloco `/* TOKENS_DA_MARCA */` pelas cores/fontes reais
     (`--bg --fg --muted --accent --accent-2 --font-display --font-body`). Sem `marca/tokens.css`,
     mantém os defaults premium e marca "a confirmar";
   - adiciona o CSS dos mockups (de `references/blocos.md`) ao `<style>`;
   - monta os slides a partir de `references/blocos.md`, com a copy aprovada, no `<!-- SLIDES -->`;
   - copia os assets reais de `producao/` pra `producao/slides/<tema>/assets/`;
   - gera `producao/slides/<tema>/notas.md` (as `data-notes` de cada slide, em ordem) pro segundo monitor.

8. **Verifica:** `node .claude/skills/slides/references/verificar.mjs producao/slides/<tema>/deck.html
   producao/slides/<tema>/_shots`. Lê os screenshots; se algum slide vier quebrado/preto ou fora
   da marca, corrige. (Deletar `_shots/` depois, ou deixar pro dono ver.)

9. **Abre no navegador** pra conferência ao vivo: `start producao/slides/<tema>/deck.html`
   (Windows). Lembra os controles: `→`/`←` passa, `F` tela cheia, `S` notas, `B` preto.

**Modo institucional** = deck-base único, atualizado a cada rodada. **Modo tema** = pasta nova por vídeo.

## Encaixe no sistema
Opcional (o guia não empurra), eixo vídeo. Pré-requisito mínimo: `marca/` — sem ela, reorienta
pro `/identidade`. Entra avulsa ou depois de `/roteiro-yt`·/tema-yt. Sai pro `/gravar-tela`.

Reusa: mockups phone/browser (mesmo padrão do `/reel-marca`), `/escritor-br` (humaniza),
`/provas` (prova real), regra "1 herói / answer-first" da `/pagina` e do reel.

## Saída
`producao/slides/<tema>/deck.html` (auto-contido, abre offline) + `assets/` + `notas.md`. A
produção fica no clone; o motor (engine, blocos, verificar) desce do template via `/atualizar-motor`.

---

**✓ Pronto:** deck premium na marca, navegável em tela cheia, com produto real e notas do
apresentador · **→ próximo passo:** `/gravar-tela` — grava a tela passando o deck e alternando
pro Claude Code. Pré-requisito: `marca/`; se faltar, o sistema reorienta pro `/identidade`.
```

- [ ] **Step 2: Validar o YAML frontmatter**

Run:
```bash
node -e "const fs=require('fs');const t=fs.readFileSync('.claude/skills/slides/SKILL.md','utf8');const m=t.match(/^---\n([\s\S]*?)\n---/);if(!m){console.error('SEM frontmatter');process.exit(1)}const y=m[1];if(!/name:\s*slides/.test(y)){console.error('name errado');process.exit(1)}if(!/description:/.test(y)){console.error('sem description');process.exit(1)}console.log('frontmatter ok')"
```
Expected: imprime `frontmatter ok`.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/slides/SKILL.md
git commit -m "feat(slides): SKILL.md — fluxo guiado com 2 gates

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Registrar no mapa de skills + bump de versão

Costura a skill no sistema: esteira, tabela de fluxo, versão.

**Files:**
- Modify: `docs/mapa-de-skills.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: Adicionar `/slides` à esteira de YouTube/vídeo no mapa**

Em `docs/mapa-de-skills.md`, na seção "A esteira de YOUTUBE", após o diagrama existente, adicionar o parágrafo:

```markdown
- **/slides** (opcional, eixo vídeo) — deck de apresentação premium na marca pra rodar no PC
  durante a gravação: slides em tela cheia, produto real em mockup, slides-ponte pra demo ao vivo
  no Claude Code e notas do apresentador. Entra avulsa ou depois de `/roteiro-yt`·/tema-yt; sai
  pro `/gravar-tela`. Distinta do `/reel-marca` (vídeo que toca sozinho) — aqui o dono navega ao vivo.
```

- [ ] **Step 2: Adicionar `/slides` à tabela de fluxo guiado**

Em `docs/mapa-de-skills.md`, na "Tabela de fluxo guiado", adicionar a linha (logo após a linha do `/tema-yt` ou junto do bloco de vídeo):

```markdown
| /slides | /gravar-tela | **marca/ (senão: rodar /identidade antes)** |
```

- [ ] **Step 3: Adicionar `/slides` ao bloco PRINCIPAL × OPCIONAIS**

Em `docs/mapa-de-skills.md`, na lista de OPCIONAIS, na linha do YouTube ou logo abaixo, registrar que `/slides` é apoio de vídeo opcional:

```markdown
   • Apresentação/slides  (/slides → /gravar-tela)  — deck premium pra gravar vídeo
```

- [ ] **Step 4: Bump de versão no CLAUDE.md**

Em `CLAUDE.md`, na última linha, trocar:
```
*ImpulsoX-OS · produto da ImpulsoX AI · impulsoxai.com.br · v0.2.6*
```
por:
```
*ImpulsoX-OS · produto da ImpulsoX AI · impulsoxai.com.br · v0.2.7*
```

- [ ] **Step 5: Verificar que o mapa cita `/slides` nos 3 lugares**

Run:
```bash
grep -c "/slides" docs/mapa-de-skills.md
```
Expected: `3` (ou mais).

- [ ] **Step 6: Commit**

```bash
git add docs/mapa-de-skills.md CLAUDE.md
git commit -m "feat(slides): registrar no mapa de skills + bump v0.2.7

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: Smoke test end-to-end (institucional) + memória

Provar o fluxo completo gerando um deck institucional de teste no scratchpad, e registrar a skill na memória.

**Files:**
- Verify (scratchpad): `…/scratchpad/inst/deck.html`
- Create: `C:\Users\ACER\.claude\projects\c--Users-ACER-Desktop-ImpulsoX-OS\memory\skill-slides.md`
- Modify: `C:\Users\ACER\.claude\projects\c--Users-ACER-Desktop-ImpulsoX-OS\memory\MEMORY.md`

- [ ] **Step 1: Montar um deck institucional de teste no scratchpad**

Seguindo o fluxo do `SKILL.md` à mão (modo institucional): copie `engine.html` pra
`$SCRATCH/inst/deck.html`, injete cores ImpulsoX (roxo `--accent:#a974ff`, dourado
`--accent-2:#e9cd6a`, fundo `#080510`), monte ~6 slides: capa "ImpulsoX AI", conteúdo "IA que
trabalha na sua PME", produto-real (um carrossel real de `ImpulsoX-AI/producao/posts/` em phone —
copie um `slide-*.png` pra `$SCRATCH/inst/assets/`), ponte-demo "gerar landing page ao vivo",
conteúdo "na sua voz, sem cara de IA", assinatura "impulsoxai.com.br". Cada um com `data-notes`.

- [ ] **Step 2: Rodar a verificação e auditar visualmente**

Run:
```bash
node .claude/skills/slides/references/verificar.mjs "$SCRATCH/inst/deck.html" "$SCRATCH/inst/_shots"
```
Expected: `slides: 6`, gera os screenshots. Abrir (Read) `slide-01.png`..`slide-06.png` + `presenter.png` e confirmar:
- cores ImpulsoX aplicadas (roxo/dourado sobre fundo escuro);
- o carrossel real aparece dentro do phone-mockup, não cortado/preto;
- a ponte-demo tem "▶ demo ao vivo";
- `presenter.png` mostra a nota do slide atual + prévia do próximo.
Corrigir engine/blocos se algo quebrar; re-rodar.

- [ ] **Step 3: Confirmar que `notas.md` seria gerável (extrair as data-notes)**

Run:
```bash
node -e "const{chromium}=require('playwright');(async()=>{const b=await chromium.launch();const p=await b.newPage();await p.goto('file://'+(process.env.SCRATCH+'/inst/deck.html').replace(/\\\\/g,'/'));const notes=await p.\$\$eval('.slide',ss=>ss.map((s,i)=>(i+1)+'. '+(s.dataset.notes||'')));console.log(notes.join('\n'));await b.close();})()"
```
Expected: imprime 6 linhas numeradas com as notas — confirma que o deck carrega as notas e o `notas.md` é só esse dump.

- [ ] **Step 4: Limpar o scratchpad (não commitar deck de teste)**

Run:
```bash
rm -rf "$SCRATCH/inst" "$SCRATCH/shots" "$SCRATCH"/s0.png "$SCRATCH"/deck-*.html
```
Expected: sem erro. `git status` não deve listar nada em `producao/` nem no scratchpad.

- [ ] **Step 5: Escrever a memória da skill**

Criar `…/memory/skill-slides.md`:

```markdown
---
name: skill-slides
description: Skill /slides — deck de apresentação premium (HTML navegável tela cheia) pra rodar no PC durante gravação de vídeo; produto real em mockup + slides-ponte pra demo ao vivo no Claude Code + notas do apresentador
metadata:
  type: project
---

`/slides` (v0.2.7, criada 2026-06-23): gera deck premium na marca pra gravar vídeo. HTML
auto-contido (`engine.html`): navegação por seta, `F` tela cheia, `S` presenter view, `B`
blackout; tokens da marca injetados de `marca/tokens.css` (defaults premium escuros sem marca).
5 tipos de slide em `blocos.md` (capa/conteúdo/produto-real/ponte-demo/assinatura). 2 gates
(storyboard, copy) como o [[reel-remotion-formula]]. Verificação visual via Playwright
(`verificar.mjs` → screenshot de cada slide + presenter). Modo institucional (pitch ImpulsoX)
+ modo tema. Saída em `producao/slides/<tema>/`. Distinta do /reel-marca (vídeo sozinho) — aqui
o dono navega ao vivo e alterna pro Claude Code mostrar o produto real. Sai pro /gravar-tela.

**Why:** o dono grava vídeos alternando slides ↔ tela do Claude Code; precisava de slides na
marca, nível-agência, sem PowerPoint genérico.
**How to apply:** próximo deck = ler voz+ofertas+marca → detectar entrada (tema/roteiro/nada=
institucional) → storyboard (GATE 1) → copy via /escritor-br só oferta ATIVA (GATE 2) → montar
engine+blocos com produto real de producao/ → verificar com Playwright → abrir no navegador.
```

- [ ] **Step 6: Adicionar a linha no índice `MEMORY.md`**

Adicionar ao fim de `…/memory/MEMORY.md`:
```markdown
- [Skill /slides](skill-slides.md) — deck de apresentação premium pra gravar vídeo (navegável, produto real, notas do apresentador); v0.2.7, distinta do /reel-marca
```

- [ ] **Step 7: Commit final**

```bash
git add docs/superpowers/plans/2026-06-23-slides.md
git commit -m "docs(plan): /slides — plano de implementação

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```
(A memória fica fora do git do projeto — é do diretório `.claude`.)

---

## Notas de implementação

- **Windows:** `start <arquivo>` abre no navegador padrão; `cp`/`rm` rodam no Bash tool (Git Bash). Caminhos com `\` viram `/` em `file://`.
- **Playwright:** já é dependência do projeto (usado por `/pagina`, `/reel-marca`). Se `require('playwright')` não resolver na raiz do OS, rodar de `ImpulsoX-AI/` ou via `npx`.
- **`marca/tokens.css` não existe no template** (é por-clone). O engine traz defaults premium; a injeção da marca só acontece em runtime no clone. Os testes deste plano usam cores ImpulsoX hard-coded no scratchpad — não dependem do arquivo.
- **Nada em `producao/` é commitado** por este plano: tudo de teste vai pro scratchpad e é apagado. O plano entrega só o motor (skill + references) + registro no mapa.
```