# B-roll com Voz Narrada — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar voz narrada (real ou TTS) ao gerar-video.mjs, onde a fala de cada cena manda a duração do clipe B-roll, com mix de voz+trilha e karaokê sincronizado.

**Architecture:** Estende `scripts/gerar-video.mjs` com funções puras (`duracaoAudio`, `casarDuracoes`, `mixVozTrilha`) e flags `--voz`/`--tts`. Cria `scripts/gerar-tts.mjs` isolado (ElevenLabs + `estimarCustoTTS`). Reusa `transcrever` (transcrever-local.mjs) e `montarASS` (lib-edicao.mjs) pro karaokê. Sem voz = comportamento atual intacto.

**Tech Stack:** Node ESM, ffmpeg/ffprobe (binário), Fal (vídeo), ElevenLabs (TTS via fetch), Whisper local, `node --test`.

---

## File Structure

- `scripts/gerar-video.mjs` (MODIFICAR) — funções puras novas (`duracaoAudio`, `casarDuracoes`,
  `mixVozTrilha`) + flags `--voz`/`--tts` no bloco CLI. Sem voz = caminho atual.
- `scripts/gerar-video.test.mjs` (MODIFICAR) — testes das 3 funções puras novas.
- `scripts/gerar-tts.mjs` (CRIAR) — `estimarCustoTTS` (pura) + `gerarTTS` (ElevenLabs via fetch) + CLI.
- `scripts/gerar-tts.test.mjs` (CRIAR) — teste de `estimarCustoTTS`.
- `.claude/skills/post/SKILL.md` (MODIFICAR) — documenta o modo "reel narrado".

> Reuso: `transcrever(audio, opts)` de `scripts/transcrever-local.mjs`; `montarASS(palavras)` de
> `scripts/lib-edicao.mjs`. Não reimplementar.

---

### Task 1: `duracaoAudio` — parseia a duração do ffprobe

**Files:**
- Modify: `scripts/gerar-video.mjs` (adicionar a função perto de `custoClipe`, ~linha 75)
- Test: `scripts/gerar-video.test.mjs`

- [ ] **Step 1: Escrever o teste falhando** (adicionar em `scripts/gerar-video.test.mjs`)

```javascript
import { duracaoAudio } from "./gerar-video.mjs";

test("duracaoAudio extrai segundos da saída do ffprobe", () => {
  assert.equal(duracaoAudio("duration=3.456000\n"), 3.456);
});
test("duracaoAudio devolve 0 quando não acha duração", () => {
  assert.equal(duracaoAudio("lixo sem duracao"), 0);
});
```

- [ ] **Step 2: Rodar pra ver falhar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node --test scripts/gerar-video.test.mjs`
Expected: FAIL — `duracaoAudio is not a function` / import inexistente.

- [ ] **Step 3: Implementar** (em `scripts/gerar-video.mjs`, após `custoClipe`)

```javascript
// duracaoAudio — lê a duração (s) da saída de `ffprobe -show_entries format=duration`.
// Função pura: recebe a STRING de saída, não roda ffprobe. Devolve 0 se não achar.
export function duracaoAudio(saidaFfprobe) {
  const m = String(saidaFfprobe).match(/duration=([\d.]+)/);
  return m ? Number(m[1]) : 0;
}
```

- [ ] **Step 4: Rodar pra ver passar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node --test scripts/gerar-video.test.mjs`
Expected: PASS (os 2 testes novos verdes + os 9 antigos).

- [ ] **Step 5: Commit**

```bash
git add scripts/gerar-video.mjs scripts/gerar-video.test.mjs
git commit -m "feat(broll-voz): duracaoAudio — lê duração da fala do ffprobe"
```

---

### Task 2: `casarDuracoes` — a fala manda a duração da cena

**Files:**
- Modify: `scripts/gerar-video.mjs`
- Test: `scripts/gerar-video.test.mjs`

- [ ] **Step 1: Escrever o teste falhando**

```javascript
import { casarDuracoes } from "./gerar-video.mjs";

test("casarDuracoes aplica a duração da fala (+folga) em cada cena", () => {
  const cenas = [{ visual: "a" }, { visual: "b" }];
  const out = casarDuracoes(cenas, [2.0, 3.5], { folga: 0.4 });
  assert.equal(out[0].segundos, 2.4);
  assert.equal(out[1].segundos, 3.9);
  assert.equal(out[0].visual, "a"); // preserva o resto da cena
});

test("casarDuracoes mantém segundos existente quando não há áudio pra cena", () => {
  const cenas = [{ visual: "a", segundos: 5 }];
  const out = casarDuracoes(cenas, [0], { folga: 0.4 });
  assert.equal(out[0].segundos, 5); // duração 0 = sem fala, mantém o fixo
});
```

- [ ] **Step 2: Rodar pra ver falhar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node --test scripts/gerar-video.test.mjs`
Expected: FAIL — `casarDuracoes is not a function`.

- [ ] **Step 3: Implementar**

```javascript
// casarDuracoes — sobrescreve cada cena.segundos com a duração da fala (+folga), pra o clipe
// B-roll nunca cortar a voz. Duração 0 (sem áudio) mantém o segundos existente. Pura.
export function casarDuracoes(cenas, duracoesAudio, { folga = 0.4 } = {}) {
  return cenas.map((c, i) => {
    const d = Number(duracoesAudio[i]) || 0;
    return d > 0 ? { ...c, segundos: Math.round((d + folga) * 100) / 100 } : { ...c };
  });
}
```

- [ ] **Step 4: Rodar pra ver passar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node --test scripts/gerar-video.test.mjs`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add scripts/gerar-video.mjs scripts/gerar-video.test.mjs
git commit -m "feat(broll-voz): casarDuracoes — a fala manda a duração do clipe"
```

---

### Task 3: `mixVozTrilha` — args ffmpeg pra mixar voz (alto) + música (baixo)

**Files:**
- Modify: `scripts/gerar-video.mjs`
- Test: `scripts/gerar-video.test.mjs`

- [ ] **Step 1: Escrever o teste falhando**

```javascript
import { mixVozTrilha } from "./gerar-video.mjs";

test("mixVozTrilha mixa voz e música com a música mais baixa", () => {
  const a = mixVozTrilha({ video: "v.mp4", voz: "voz.mp3", trilha: "m.mp3", saida: "out.mp4", duckDb: 18 });
  // 3 inputs: video, voz, trilha
  assert.deepEqual(a.slice(0, 6), ["-y", "-i", "v.mp4", "-i", "voz.mp3", "-i"]);
  // a trilha leva volume negativo (abaixa); amix junta voz+trilha
  assert.ok(a.join(" ").includes("volume=-18dB"));
  assert.ok(a.join(" ").includes("amix=inputs=2"));
  assert.ok(a.join(" ").includes("out.mp4"));
});

test("mixVozTrilha sem trilha usa só a voz", () => {
  const a = mixVozTrilha({ video: "v.mp4", voz: "voz.mp3", saida: "out.mp4" });
  assert.deepEqual(a.slice(0, 5), ["-y", "-i", "v.mp4", "-i", "voz.mp3"]);
  assert.ok(!a.join(" ").includes("amix")); // sem mix, só mapeia a voz
  assert.ok(a.join(" ").includes("1:a"));
});
```

- [ ] **Step 2: Rodar pra ver falhar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node --test scripts/gerar-video.test.mjs`
Expected: FAIL — `mixVozTrilha is not a function`.

- [ ] **Step 3: Implementar**

```javascript
// mixVozTrilha — monta os args do ffmpeg pra juntar o vídeo (mudo) com a VOZ no volume cheio e,
// se houver, a TRILHA abaixada (duckDb dB) por baixo. Pura (só monta args, não roda ffmpeg).
export function mixVozTrilha({ video, voz, trilha, saida, duckDb = 18 }) {
  const inputs = ["-i", video, "-i", voz];
  if (trilha) inputs.push("-i", trilha);
  const args = ["-y", ...inputs];
  if (trilha) {
    // abaixa a trilha e mistura com a voz; dropout_transition evita o amix "respirar"
    args.push(
      "-filter_complex",
      `[2:a]volume=-${duckDb}dB[m];[1:a][m]amix=inputs=2:duration=first:dropout_transition=0[aout]`,
      "-map", "0:v", "-map", "[aout]",
    );
  } else {
    args.push("-map", "0:v", "-map", "1:a");
  }
  args.push("-c:v", "copy", "-c:a", "aac", "-shortest", saida);
  return args;
}
```

- [ ] **Step 4: Rodar pra ver passar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node --test scripts/gerar-video.test.mjs`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add scripts/gerar-video.mjs scripts/gerar-video.test.mjs
git commit -m "feat(broll-voz): mixVozTrilha — voz no volume cheio + trilha abaixada"
```

---

### Task 4: `gerar-tts.mjs` — TTS isolado (ElevenLabs) + estimativa de custo

**Files:**
- Create: `scripts/gerar-tts.mjs`
- Test: `scripts/gerar-tts.test.mjs`

- [ ] **Step 1: Escrever o teste falhando** (`scripts/gerar-tts.test.mjs`)

```javascript
import { test } from "node:test";
import assert from "node:assert/strict";
import { estimarCustoTTS } from "./gerar-tts.mjs";

test("estimarCustoTTS calcula ~$0.30 por 1000 chars", () => {
  assert.equal(estimarCustoTTS("a".repeat(1000)), 0.30);
  assert.equal(estimarCustoTTS("a".repeat(500)), 0.15);
});

test("estimarCustoTTS soma várias falas (array)", () => {
  // 1000 + 1000 chars = $0.60
  assert.equal(estimarCustoTTS(["a".repeat(1000), "b".repeat(1000)]), 0.60);
});

test("estimarCustoTTS de texto vazio é 0", () => {
  assert.equal(estimarCustoTTS(""), 0);
  assert.equal(estimarCustoTTS([]), 0);
});
```

- [ ] **Step 2: Rodar pra ver falhar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node --test scripts/gerar-tts.test.mjs`
Expected: FAIL — "Cannot find module './gerar-tts.mjs'".

- [ ] **Step 3: Implementar** (`scripts/gerar-tts.mjs`)

```javascript
#!/usr/bin/env node
// gerar-tts.mjs — locução por IA (ElevenLabs). Isolado: trocar de provedor depois não mexe no
// gerar-video. Chave em ELEVENLABS_KEY (.env, gitignored). estimarCustoTTS é pura/testável.
// Uso: ELEVENLABS_KEY=... node scripts/gerar-tts.mjs "texto da fala" --voz <voiceId> --saida fala.mp3
import { writeFileSync } from "node:fs";

const PRECO_POR_1K = 0.30; // ~$0.30/1000 chars (ElevenLabs, jun/2026)

// estima o custo em USD de uma fala (string) ou várias (array de strings). Pura.
export function estimarCustoTTS(texto) {
  const arr = Array.isArray(texto) ? texto : [texto];
  const chars = arr.reduce((n, t) => n + String(t || "").length, 0);
  return Math.round((chars / 1000) * PRECO_POR_1K * 100) / 100;
}

// gera o mp3 de uma fala via ElevenLabs. Faz I/O (rede+disco) — não testada em unidade.
export async function gerarTTS({ texto, voz, saida, key = process.env.ELEVENLABS_KEY, fetchFn = fetch }) {
  if (!key) throw new Error("ELEVENLABS_KEY não definido no .env.");
  if (!voz) throw new Error("informe --voz <voiceId> (voz pronta ou já clonada no provedor).");
  const r = await fetchFn(`https://api.elevenlabs.io/v1/text-to-speech/${voz}`, {
    method: "POST",
    headers: { "xi-api-key": key, "Content-Type": "application/json" },
    body: JSON.stringify({ text: texto, model_id: "eleven_multilingual_v2" }),
  });
  if (!r.ok) throw new Error(`ElevenLabs ${r.status}: ${await r.text()}`);
  const buf = Buffer.from(await r.arrayBuffer());
  writeFileSync(saida, buf);
  return saida;
}

if (import.meta.main) {
  const args = process.argv.slice(2);
  const flag = (n) => { const i = args.indexOf(n); return i !== -1 ? args[i + 1] : undefined; };
  const texto = args.find((a, i) => !a.startsWith("--") && args[i - 1] !== "--voz" && args[i - 1] !== "--saida");
  const voz = flag("--voz");
  const saida = flag("--saida") || "fala.mp3";
  if (!texto) { console.error("informe o texto da fala."); process.exit(1); }
  console.log(`custo estimado: $${estimarCustoTTS(texto).toFixed(2)}`);
  gerarTTS({ texto, voz, saida })
    .then((s) => console.log("gerado: " + s))
    .catch((e) => { console.error("ERRO: " + e.message); process.exit(1); });
}
```

- [ ] **Step 4: Rodar pra ver passar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node --test scripts/gerar-tts.test.mjs`
Expected: PASS (3 testes verdes).

- [ ] **Step 5: Commit**

```bash
git add scripts/gerar-tts.mjs scripts/gerar-tts.test.mjs
git commit -m "feat(broll-voz): gerar-tts.mjs — locução ElevenLabs isolada + custo"
```

---

### Task 5: Integrar voz no pipeline do gerar-video (CLI)

**Files:**
- Modify: `scripts/gerar-video.mjs` (bloco `if (import.meta.main)`, ~linha 78 em diante)

> Integração de I/O (Fal/ffmpeg/Whisper reais) — não tem teste de unidade; validada pelo dono
> em uso real (dry-run primeiro). As funções puras que ela orquestra já têm teste (Tasks 1-4).

- [ ] **Step 1: Adicionar a leitura das flags e a obtenção das falas**

No bloco `if (import.meta.main)`, após a leitura do roteiro (onde hoje pega `--modelo`/`--dry-run`),
adicionar:

```javascript
  const vozDir = flag("--voz");   // pasta com cena-01.mp3, cena-02.mp3... (voz real)
  const ttsVoz = flag("--tts");   // voiceId do ElevenLabs (voz por IA)
  const trilha = flag("--trilha");
  const temVoz = Boolean(vozDir || ttsVoz);

  // resolve o áudio de cada cena: voz real (arquivo na pasta) OU TTS (gera).
  // Importa só quando precisa (evita custo/dep à toa).
  let falasAudio = []; // caminho do mp3 de cada cena, na ordem
  if (temVoz) {
    const { join: pjoin } = await import("node:path");
    const baseFalas = pjoin("canal-youtube", "broll", roteiro.slug, "falas");
    mkdirSync(baseFalas, { recursive: true });
    if (ttsVoz) {
      const { gerarTTS, estimarCustoTTS } = await import("./gerar-tts.mjs");
      const textos = roteiro.cenas.map((c) => c.narracao || "");
      const custo = estimarCustoTTS(textos);
      console.log(`TTS: ${textos.length} falas, custo estimado $${custo.toFixed(2)}.`);
      if (!has("--confirmar")) { console.log("rode com --confirmar pra gerar o TTS (gasta crédito)."); process.exit(0); }
      for (let i = 0; i < roteiro.cenas.length; i++) {
        const saida = pjoin(baseFalas, `cena-${String(i + 1).padStart(2, "0")}.mp3`);
        await gerarTTS({ texto: roteiro.cenas[i].narracao || "", voz: ttsVoz, saida });
        falasAudio.push(saida);
      }
    } else {
      // voz real: espera cena-01.mp3... na pasta --voz
      for (let i = 0; i < roteiro.cenas.length; i++) {
        const arq = pjoin(vozDir, `cena-${String(i + 1).padStart(2, "0")}.mp3`);
        if (!existsSync(arq)) falhar(`falta o áudio da cena ${i + 1}: ${arq}`);
        falasAudio.push(arq);
      }
    }
  }
```

- [ ] **Step 2: Medir as falas e casar as durações antes de gerar os clipes**

ANTES do loop que gera os clipes Fal (onde hoje usa `cena.segundos`), inserir:

```javascript
  if (temVoz) {
    const duracoes = falasAudio.map((a) => {
      const r = spawnSync("ffprobe", ["-v", "error", "-show_entries", "format=duration", "-of", "default=noprint_wrappers=1", a], { encoding: "utf8" });
      return duracaoAudio(r.stdout || "");
    });
    roteiro.cenas = casarDuracoes(roteiro.cenas, duracoes);
    console.log("durações casadas com a voz: " + roteiro.cenas.map((c) => c.segundos + "s").join(", "));
  }
```

(Garantir que `spawnSync` está importado de `node:child_process` no topo — adicionar ao import
existente se faltar.)

- [ ] **Step 3: Mixar a voz e aplicar karaokê após a costura**

DEPOIS do `execFileSync("ffmpeg", argsFfmpeg(...))` que costura os clipes (gera o vídeo mudo,
chame-o de `videoMudo`), inserir:

```javascript
  if (temVoz) {
    // 1) junta as falas numa trilha de voz contínua (concat dos mp3 das cenas)
    const { join: pjoin } = await import("node:path");
    const baseOut = pjoin("canal-youtube", "broll", roteiro.slug);
    const vozUnica = pjoin(baseOut, "_voz.mp3");
    const listaVoz = pjoin(baseOut, "_voz.txt");
    writeFileSync(listaVoz, falasAudio.map((a) => `file '${a.replace(/\\/g, "/")}'`).join("\n"));
    execFileSync("ffmpeg", ["-y", "-f", "concat", "-safe", "0", "-i", listaVoz, "-c", "copy", vozUnica], { stdio: "inherit" });

    // 2) mixa voz (+trilha opcional abaixada) sobre o vídeo mudo
    const comVoz = pjoin(baseOut, "_comvoz.mp4");
    execFileSync("ffmpeg", mixVozTrilha({ video: saida, voz: vozUnica, trilha, saida: comVoz }), { stdio: "inherit" });

    // 3) karaokê: transcreve a voz e queima a legenda palavra-a-palavra (reusa o motor existente)
    try {
      const { transcrever } = await import("./transcrever-local.mjs");
      const { montarASS, filtroLegendaAss } = await import("./lib-edicao.mjs");
      const palavras = transcrever(vozUnica);
      if (palavras.length) {
        const ass = pjoin(baseOut, "_karaoke.ass");
        writeFileSync(ass, montarASS(palavras));
        execFileSync("ffmpeg", ["-y", "-i", comVoz, "-vf", filtroLegendaAss({ assCaminho: ass }), "-c:a", "copy", saida], { stdio: "inherit" });
      } else {
        renameSync(comVoz, saida); // sem transcrição: fica a voz sem karaokê
      }
    } catch (e) {
      console.error("AVISO: karaokê pulado — " + e.message);
      renameSync(comVoz, saida);
    }
  }
```

(Garantir imports no topo: `writeFileSync`, `renameSync`, `mkdirSync`, `existsSync` de `node:fs`;
`execFileSync`, `spawnSync` de `node:child_process`. Adicionar os que faltarem.)

- [ ] **Step 4: Verificar que o caminho SEM voz não regrediu**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && node scripts/gerar-video.mjs --help 2>&1 | head -3; node --test scripts/gerar-video.test.mjs 2>&1 | grep -iE "pass|fail"`
Expected: o script carrega sem erro de sintaxe; testes das funções puras seguem verdes.

- [ ] **Step 5: Dry-run de sanidade (sem chave, modo voz real ausente)**

Criar um roteiro mínimo de teste e rodar dry-run pra confirmar que sem `--voz`/`--tts` o fluxo
é o de antes:

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && printf '{"slug":"t","cenas":[{"texto":"oi","visual":"praia","segundos":3}]}' > c:/tmp/rot.json && node scripts/gerar-video.mjs c:/tmp/rot.json --dry-run 2>&1 | tail -5`
Expected: mostra o plano do modo atual (sem voz), sem erro.

- [ ] **Step 6: Commit**

```bash
git add scripts/gerar-video.mjs
git commit -m "feat(broll-voz): pipeline de voz no gerar-video (fala manda duração + karaokê)"
```

---

### Task 6: Documentar o modo "reel narrado" no /post + memória

**Files:**
- Modify: `.claude/skills/post/SKILL.md`

- [ ] **Step 1: Adicionar a seção do modo narrado**

Em `.claude/skills/post/SKILL.md`, na parte que descreve o reel (procurar a seção que fala de
reel/vídeo), adicionar uma subseção:

```markdown
## Reel narrado (B-roll com voz, zero-filmagem)

Além do reel com rosto por IA, dá pra fazer um reel onde uma VOZ narra sobre clipes
cinematográficos gerados por IA — o dono não filma nada. Cada cena do roteiro ganha um campo
`narracao` (o que a voz fala). A voz vem de dois jeitos (Escada de Contexto):

- **Voz real:** o dono grava cada fala (`cena-01.mp3`, `cena-02.mp3`…) numa pasta e passa
  `--voz <pasta>`. Grátis, autêntico.
- **Voz por IA (TTS):** `--tts <voiceId>` gera a locução (ElevenLabs). Tem guarda de custo
  (`--confirmar`) e `ELEVENLABS_KEY` no `.env`. Clonar a própria voz = ok; voz de terceiro sem
  permissão = não.

A fala manda a duração: cada clipe dura o tempo da narração da cena (nunca corta a voz). A
legenda é karaokê sincronizado (Whisper transcreve a voz). Música opcional entra abaixada por
baixo da voz (`--trilha`). Sempre `--dry-run` primeiro (mostra cenas, durações, custo); peça
vai ao ar → passa por `/revisar`.

Comando: `node scripts/gerar-video.mjs <roteiro.json> --voz <pasta>` (real) ou `--tts <voiceId>
--confirmar` (IA), com `--trilha <musica.mp3>` opcional.
```

- [ ] **Step 2: Verificar**

Run: `cd "C:/Users/ACER/Desktop/ImpulsoX-OS" && grep -n "Reel narrado" .claude/skills/post/SKILL.md`
Expected: 1 linha.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/post/SKILL.md
git commit -m "docs(post): documenta o modo reel narrado (B-roll com voz)"
```

---

## Self-Review (preenchido)

**Spec coverage:**
- Campo `narracao` por cena → usado na Task 5 (TTS lê `c.narracao`). ✓
- Duas fontes de voz (`--voz` real / `--tts` IA) → Task 5 Step 1. ✓
- A fala manda a duração (`duracaoAudio` + `casarDuracoes`) → Tasks 1, 2, 5 Step 2. ✓
- Mix voz+trilha (`mixVozTrilha`) → Tasks 3, 5 Step 3. ✓
- Karaokê via Whisper (reusa transcrever + montarASS) → Task 5 Step 3. ✓
- TTS isolado + custo + `--confirmar` (`gerar-tts.mjs`, `estimarCustoTTS`) → Tasks 4, 5 Step 1. ✓
- Salvaguarda voz clonada (própria ok / terceiro não) → doc na Task 6 + aviso; o código aceita
  qualquer voiceId (a regra é humana/processo, não automatizável). ✓
- Sem voz = zero regressão → Task 5 Steps 4-5 (verificação). ✓
- Doc no /post → Task 6. ✓

**Placeholder scan:** nenhum TBD/TODO. Todo código por extenso.

**Type consistency:** `duracaoAudio`/`casarDuracoes`/`mixVozTrilha`/`estimarCustoTTS`/`gerarTTS`
consistentes entre teste, implementação e uso na Task 5. Campos: `cena.narracao`, `cena.visual`,
`cena.texto`, `cena.segundos`; `falasAudio` (array de caminhos); `roteiro.slug`, `roteiro.cenas`.

**Nota:** a Task 5 assume que o bloco CLI já tem `roteiro`, `flag`, `has`, `falhar`, `execFileSync`,
`existsSync` (confirmado no arquivo atual). Os imports faltantes (`spawnSync`, `renameSync`) estão
sinalizados pra adicionar nos Steps 2-3.
